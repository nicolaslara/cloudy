# Copy to .env for local overrides. Defaults in cloudy/config.py match docker-compose.
DATABASE_URL=postgresql+psycopg://cloudy:cloudy@localhost:5432/cloudy
LOG_LEVEL=INFO
# API_DOCS=false  # 404 /docs, /redoc, and /openapi.json (wired routes only)
# CORS_ALLOW_ORIGINS=http://localhost:5273,http://127.0.0.1:5273
